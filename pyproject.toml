[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "genereg-eval"
version = "0.1.0"
description = "Evidence-grounded gene-regulation extraction and evaluation"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

